projection;
strict ( 2 );
use draft;

define behavior for ZC_CUTMEnrollment_108 alias Enrollment
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action approveEnrollment;
  use action rejectEnrollment;
  use action submitEnrollment;

  use association _Subject { create; with draft; }
}

define behavior for ZC_CUTMSubject_108 alias Subject
{
  use update;
  use delete;

  use association _Enrollment { with draft; }
}